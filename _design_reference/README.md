# Design Reference — Read-Only Figma Flutter Prototype

This folder contains a **read-only snapshot** of the Figma/FlutterFlow prototype
from `/ledgerai-flutterflow-prototype/Implement LedgerAI Screens/flutter/`.

## Purpose

Visual layout and copy reference only. These files are **not compiled** into the app
and are excluded from `flutter analyze` (see root `analysis_options.yaml`).

## What's here

- `figma_flutter/lib/` — all prototype Dart source files
- `figma_flutter/pubspec.yaml` — prototype dependencies reference

## Usage

Open files here for layout inspiration, colour values, and copy wording.
Do **not** import anything from this directory into `lib/`.

## Prototype source

- **Figma prototype repo**: `/ledgerai-flutterflow-prototype/`
- **Branch**: `figma-version-2` (commit `5fc4caf`)
- **Snapshotted**: May 2026
