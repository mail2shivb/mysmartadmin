# Cursor Rules for LedgerAI

## Output Rules (non-negotiable)

Generate ONE final implementation only (no alternatives, no “version 1-4”).

Implement ONLY the step I request. Do not jump ahead.

Do not change existing working code unless required by the step.

Do not add new dependencies unless I explicitly approve them.

Do not add Firebase/cloud services.

Follow folder structure strictly (lib/app, lib/core, lib/features).

Use Material 3, premium, calm UI, consistent spacing, rounded cards.

For every change: show a file-by-file plan first, then apply changes.

Ensure build passes (flutter pub get, flutter run) after each step.

If uncertain: leave a comment and ask via TODO marker (but do not block compilation).CURSOR_RULES.md

## Coding Rules
- Keep changes minimal per step.
- No large refactors unless requested.
- If uncertain, add a comment and keep build passing.