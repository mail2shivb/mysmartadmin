## SAFE CURSOR PROMPT TEMPLATE

Implement ONLY Step <X>.
First produce a file-by-file plan (files to create/modify + why).
Then implement.
Generate ONE final solution only (no alternatives).
Do NOT add dependencies.
Do NOT refactor unrelated files.
Keep UI premium Material 3 (cards, spacing, icons, calm colors).
Ensure `flutter pub get` and `flutter run` work.
Output full contents for every created/modified file.